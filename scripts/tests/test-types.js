// if (Fire.isEditor) {
//     (function () {
//         var Types = require('types');

//         for (var i = 0; i < 52; ++i) {
//             var card = Types.Card.fromId(i);
//             if (!card) {
//                 Editor.error('Can not find card with id %s', i);
//                 return;
//             }
//         }

//         Editor.log('All tests passed');
//     })();
// }
